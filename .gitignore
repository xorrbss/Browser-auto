# Secrets & per-run artifacts — never commit
fixtures/auth/
artifacts/
*.state.json
*.webm
*.har

# Recorded input values (PII) — flow.json stores {{input_N}} tokens; real values live here
flows/*.values.json

# Authoring scratch — regenerated by `probe-record.sh scaffold/capture`, not source of truth
flows/*.snapshot.txt
flows/*.candidates.json
flows/*.flow.json.incomplete

# OS / editor
Thumbs.db
.DS_Store
